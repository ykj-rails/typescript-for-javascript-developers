let a = 1 + 2
let b = a + 3
let c = {
  apple: a,
  banana: b
}

let boolean = true

let me: { name: string; age: number; kikon: boolean; tel: number } = {
  name: 'ore',
  age: 32,
  kikon: false,
  tel: 090
}

function xxx(b: number) {
  return b / 2
}

a('z')
